package model.manager.exports.columns;

/**
 * Export column modifier used to specify which items from a list to export.
 */
public enum ColumnModifier {

	MODIFIER_NEWEST, MODIFIER_NEWEST_NUM, MODIFIER_OLDEST, MODIFIER_OLDEST_NUM, MODIFIER_FIRST_AND_LAST, MODIFIER_SHOW_ACTIVE_ONLY;

}
